/// <summary>
/// Implementing class allows interaction through up and down movement
/// </summary>
public interface IEntrance
{
    void MoveUp(Character character);
    void MoveDown(Character character);
}
